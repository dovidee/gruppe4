// Editorial content (person, social, home, about, blog, work, gallery) now lives in
// Sanity — see @/sanity/content. This file only re-exports static site configuration.
export {
  display,
  mailchimp,
  routes,
  paths,
  protectedRoutes,
  baseURL,
  fonts,
  style,
  socialSharing,
  effects,
  dataStyle,
} from "./once-ui.config";
